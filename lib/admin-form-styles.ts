/** Wrapper class — pair with rules in app/globals.css (.admin-form-fields). */
export const ADMIN_FORM_FIELDS_CLASS = "admin-form-fields";

export const ADMIN_FIELD_CLASS =
  "border-gray-200 bg-white text-gray-900 dark:border-zinc-700 dark:bg-zinc-800 dark:text-zinc-100";

/** Chips / badges inside admin forms (e.g. selected grape varieties). */
export const ADMIN_BADGE_CLASS = ADMIN_FIELD_CLASS;

export const ADMIN_LABEL_CLASS = "text-gray-700 dark:text-zinc-300";

export const ADMIN_HELP_TEXT_CLASS =
  "text-xs text-gray-500 dark:text-zinc-400";

export const ADMIN_TOGGLE_ROW_CLASS =
  "admin-toggle-row flex items-center justify-between";

/** Green = on/active, red = off/inactive (shop visibility, pallet stock, etc.). */
export const ADMIN_ACTIVE_SWITCH_CLASS =
  "data-[state=checked]:bg-emerald-600 dark:data-[state=checked]:bg-emerald-500 data-[state=unchecked]:bg-red-500 dark:data-[state=unchecked]:bg-red-600";
